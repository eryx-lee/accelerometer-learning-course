---
title: "Final Dataset in Stata"
description: "Merge person-, day-, and night-level accelerometer outputs in Stata, audit eligibility, and save a reproducible final dataset."
page-layout: full
toc: false
---

::: {.lesson-page}

::: {.lesson-hero .module-7}
::: {.lesson-hero__content}
<a class="breadcrumb-link" href="index.html">Course home</a>
<p class="eyebrow">Module 7</p>
<h1>Final Dataset in Stata</h1>
<p>Bring together file organization, GGIR settings, QC exclusions, sleep cleaning, derived bouts, and analysis-ready variables in one reproducible dataset.</p>
<div class="lesson-meta">
<span>Append and merge</span>
<span>Reliability</span>
<span>Final exclusions</span>
</div>
:::
:::

::: {.lesson-main}

::: {.objectives-panel}
<p class="section-kicker">Learning outcomes</p>
<h2>By the end of this module, you should be able to:</h2>

1. Describe the full Stata cleaning workflow from GGIR outputs to a final `.dta`.
2. Assert observation-level keys before merging person-, day-, and night-level files.
3. Explain what the Spearman–Brown projection can and cannot contribute to a valid-day decision.
4. Produce a dated final dataset, exclusion audit, dictionary, and executable do-file.
:::

::: {.table-scroll}
| Plan this module | What you need |
|---|---|
| Estimated time | 35–45 minutes, plus 25 minutes to adapt and audit the code skeleton |
| Prerequisites | Module 6 inventory/data dictionary and basic Stata data-management skills |
| Inputs | Approved person-, day-, and night-level files; QC log; protocol rules; a version-controlled do-file |
| Output | An analysis-ready `.dta`, merge audit, exclusion table, data dictionary, README, and run log |
| Completion criterion | The do-file runs from a clean Stata session and reproduces row counts, merge results, exclusions, and the final file |
:::

<nav class="lesson-jump" aria-label="Module sections">
<a href="#stata-workflow">7.1 Dataset workflow</a>
<a href="#spearman-brown">7.2 Reliability</a>
<a href="#three-parameter-rule">7.3 Exclusion rule</a>
<a href="#save-final-outputs">7.4 Final outputs</a>
<a href="#module-7-practice">7.5 Practice and self-check</a>
</nav>

::: {.lesson-section #stata-workflow}
<p class="section-kicker">Lesson 7.1</p>
<h2>Final-Dataset Workflow</h2>

The video introduces an 11-step Stata plan. The web lesson expands that plan into the following 13-step checklist so that source freezing and target-grain decisions are explicit:

1. **Freeze the inputs**: copy only approved source files into a dated, read-only analysis-input folder and preserve their original names/checksums.
2. **Import and label provenance**: record the GGIR run, source filename, threshold configuration, time window, batch, and import date.
3. **Normalize IDs without destroying the originals**: keep `id_source`, create one documented standardized ID, and verify unexpected truncation, leading zeros, and duplicates.
4. **Assert each source grain**: use `isid` on the intended key before append or merge; stop when it fails.
5. **Append like with like**: append batches only when variable definitions and GGIR configurations agree; retain a batch/source indicator.
6. **Choose the target grain**: decide whether the working dataset is one row per recording-day, recording-night, or participant before combining levels.
7. **Merge with an explicit relationship**: use the correct one-to-one, many-to-one, or one-to-many key and inspect every `_merge` category.
8. **Use the cleaned Part 4 sleep output by default**: consult the full QC file to explain exclusions; do not delete unexplained numeric codes.
9. **Reuse configured GGIR bouts when they match the estimand**: derive Stata bouts only when the required definition is absent or intentionally different.
10. **Create day-level eligibility fields**: implement the pre-specified wear, day-boundary, and weekend definitions; validate them before aggregation.
11. **Aggregate once, transparently**: produce the pre-specified participant summary while retaining an auditable day-level dataset.
12. **Apply eligibility without erasing evidence**: create reason-specific flags, report overlaps, and save the pre-exclusion dataset before restricting the analytic sample.
13. **Save dated outputs and logs**: include the final `.dta`, do-file, dictionary, merge/exclusion audit, README, and Stata log.

::: {.cohort-example}
<strong>Course example: Pelotas/UFPel cohort</strong><br>
The UFPel do-file workflow should be credited to Otavio Leão, Giulia Salaberry, and Charles Lucena. Treat it as a strong reproducible example, not a universal template: your study must decide its own ID cleaning, valid-day rules, bout definitions, and exclusion criteria.
:::

A minimal merge skeleton makes the checks executable. Replace every uppercase placeholder and inspect unmatched rows before allowing the script to continue:

```stata
version 18
clear all
capture log close
log using "logs/final_dataset_YYYYMMDD.smcl", replace

* Person/recording-level source
import delimited using "inputs/PERSON_FILE.csv", varnames(1) clear
clonevar id_source = ID
rename ID participant_id
isid participant_id
tempfile person
save `person'

* Day-level source
import delimited using "inputs/DAY_FILE.csv", varnames(1) clear
rename ID participant_id
rename CALENDAR_DATE calendar_date
isid participant_id calendar_date

* Target remains one row per participant-day: many days to one person row
merge m:1 participant_id using `person'
tabulate _merge, missing
assert _merge == 3
drop _merge

isid participant_id calendar_date
compress
save "working/day_level_pre_exclusion_YYYYMMDD.dta", replace
log close
```

`assert _merge == 3` is appropriate only after the team has confirmed that no unmatched records are expected. If an unmatched category is legitimate, replace the assertion with a documented expectation and export the affected IDs for review.
:::

::: {.lesson-section #spearman-brown}
<p class="section-kicker">Lesson 7.2</p>
<h2>Spearman-Brown Prophecy Reliability</h2>

The Spearman–Brown prophecy formula projects how reliability would change if the number of comparable repeated measurements changed:

$$
r_k = \frac{k r_1}{1 + (k - 1)r_1}
$$

where $r_1$ is the estimated reliability at the baseline measurement length and $k$ is the ratio of the proposed length to that baseline. In an accelerometer context, the repeated measurements may be valid days, but the projection is meaningful only when the reliability estimate and assumptions fit that use.

A defensible workflow is:

1. pre-specify the outcome and which days/segments are considered comparable,
2. estimate the baseline reliability with a method appropriate to the data structure,
3. state the value of $r_1$, the baseline number of days, and the proposed value of $k$,
4. calculate and report the projection with uncertainty or sensitivity analyses where appropriate,
5. consider participant burden, selection bias, weekday/weekend representation, and missingness alongside reliability.

The formula does **not** prove that days are exchangeable, correct for systematic weekday/weekend differences, or validate a minimum-day cutoff by itself. Use it as one measurement-quality input, not as an automatic exclusion command.
:::

::: {.lesson-section #three-parameter-rule}
<p class="section-kicker">Lesson 7.3</p>
<h2>Three-Parameter Exclusion Rule</h2>

For the **Pelotas/UFPel course example**, final inclusion is evaluated using three parameters:

::: {.table-scroll}
| Parameter | Example rule |
|---|---|
| Number of valid days | ≥4 valid days |
| Awake wear duration | ≥10 hours awake wear per valid day |
| Weekend coverage | At least 1 weekend day |
:::

Keep the exclusion log. It should report how many participants were excluded by each rule, how many failed multiple rules, and the final analytic sample.

::: {.review-note}
<strong>Study-specific rule:</strong> The ≥4-day, ≥10-hour, ≥1-weekend-day combination is an example protocol rule, not a GGIR default. Define “awake wear,” valid-day boundaries, weekend days, and missing values before coding. Do not use the recording-level `complete_24hcycle` score as a day-level inclusion flag.
:::

One transparent Stata pattern is to create separate flags before a combined flag. Replace the placeholder field names only after checking their units and formats:

```stata
* Requires calendar_date to be a Stata daily date and awake_wear_hours in hours
generate byte valid_day_example = awake_wear_hours >= 10 ///
    if !missing(awake_wear_hours)
generate byte weekend = inlist(dow(calendar_date), 0, 6) ///
    if !missing(calendar_date)

bysort participant_id: egen n_valid_days = total(valid_day_example)
bysort participant_id: egen n_valid_weekend = ///
    total(valid_day_example == 1 & weekend == 1)

generate byte fail_n_days = n_valid_days < 4 if !missing(n_valid_days)
generate byte fail_weekend = n_valid_weekend < 1 if !missing(n_valid_weekend)
generate byte eligible_example = fail_n_days == 0 & fail_weekend == 0

tabulate fail_n_days fail_weekend, missing
tabulate eligible_example, missing
```

Here the ≥10-hour criterion defines `valid_day_example`, so a separate participant-level “fail 10 hours” flag would double-count the same rule. If the actual protocol defines three independent participant-level conditions, encode and label each condition exactly as written in that protocol.
:::

::: {.lesson-section #save-final-outputs}
<p class="section-kicker">Lesson 7.4</p>
<h2>Save Final Outputs</h2>

Final outputs should include:

1. immutable pre-exclusion day/night dataset,
2. dated analysis-ready `.dta` file,
3. data dictionary with source and transformation lineage,
4. merge audit and unmatched-ID files,
5. reason-specific exclusion table with overlapping failures,
6. executable Stata do-file and run log,
7. README with source GGIR configuration, batch dates, Stata version, and row/ID counts.

Example file name:

```text
accelerometer_final_analysis_YYYYMMDD.dta
```

Avoid labeling a file `final_final.dta` or silently replacing a published dataset. If a result changes, create a new dated/versioned release and record why.

::: {.cohort-example}
<strong>Mini-activity: run an audit rehearsal</strong><br>
<form class="scored-quiz" data-quiz-id="module-7-mini-audit-rehearsal" data-pass-score="1" novalidate>
<fieldset data-answer="a" data-explanation="An audit rehearsal records row and ID counts before and after the merge, exports unmatched IDs, confirms that the intended key remains unique, and creates reason-specific eligibility flags before dropping any observation.">
<legend><span>1</span> Which pilot merge provides the evidence needed for an audit rehearsal?</legend>
<label><input type="radio" name="m7-mini-1" value="a"> A. Record pre/post row and distinct-ID counts, export unmatched IDs, recheck key uniqueness, and create reason-specific flags without dropping observations</label>
<label><input type="radio" name="m7-mini-1" value="b"> B. Keep only matched rows immediately, then report the final row count</label>
<label><input type="radio" name="m7-mini-1" value="c"> C. Use <code>merge m:m</code> so every possible pair appears and no key needs testing</label>
<label><input type="radio" name="m7-mini-1" value="d"> D. Review the table visually and omit the run log because the files are pilots</label>
<p class="scored-quiz__feedback" aria-live="polite" hidden></p>
</fieldset>
<div class="scored-quiz__actions">
<button type="submit" class="button button-primary">Check answer</button>
<button type="reset" class="button button-secondary">Reset and retry</button>
</div>
<p class="scored-quiz__score" role="status" tabindex="-1" hidden></p>
</form>
:::

**Completion evidence:** a collaborator can start a clean Stata session, run the do-file without manual clicking, and reproduce the recorded row counts, merge table, eligibility table, and output filename.
:::

::: {.lesson-section #module-7-practice}
<p class="section-kicker">Practice</p>
<h2>Self-check with Feedback</h2>

<form class="scored-quiz" data-quiz-id="module-7-self-check" novalidate>
<fieldset data-answer="c" data-explanation="After isid verifies the full day key and the person key separately, merge many-to-one from the day-level master data to the person-level using data. This preserves one row per day.">
<legend><span>1</span> A day-level file has seven rows per participant and a person-level file has one. Which relationship preserves one row per day?</legend>
<label><input type="radio" name="m7-check-1" value="a"> A. One-to-one on participant ID</label>
<label><input type="radio" name="m7-check-1" value="b"> B. Many-to-many on participant ID</label>
<label><input type="radio" name="m7-check-1" value="c"> C. Many-to-one from the verified day-level master to the verified person-level using data</label>
<label><input type="radio" name="m7-check-1" value="d"> D. Append the person-level record seven times before testing any key</label>
<p class="scored-quiz__feedback" aria-live="polite" hidden></p>
</fieldset>

<fieldset data-answer="d" data-explanation="No. Duplicate participant IDs usually mean that the true keys or target grain have not been identified. Resolve the unit of observation and keys rather than hiding the problem with a many-to-many merge.">
<legend><span>2</span> Both files contain duplicate participant IDs. Is <code>merge m:m</code> an acceptable shortcut?</legend>
<label><input type="radio" name="m7-check-2" value="a"> A. Yes, whenever both files have more than one row per ID</label>
<label><input type="radio" name="m7-check-2" value="b"> B. Yes, if unmatched rows will be dropped afterward</label>
<label><input type="radio" name="m7-check-2" value="c"> C. Yes, if the command runs without a Stata error</label>
<label><input type="radio" name="m7-check-2" value="d"> D. No; identify the target grain and true keys, then use the explicit valid relationship</label>
<p class="scored-quiz__feedback" aria-live="polite" hidden></p>
</fieldset>

<fieldset data-answer="b" data-explanation="A Spearman–Brown result is a conditional reliability projection, not a complete inclusion rationale. The rule must also reflect the outcome, day comparability, missingness, weekday/weekend coverage, scientific plan, and participant burden.">
<legend><span>3</span> Does a favorable Spearman–Brown projection by itself justify a four-day inclusion rule?</legend>
<label><input type="radio" name="m7-check-3" value="a"> A. Yes, because reliability is the only relevant consideration</label>
<label><input type="radio" name="m7-check-3" value="b"> B. No; it must be considered with outcome relevance, day comparability, missingness, weekday/weekend coverage, the scientific plan, and burden</label>
<label><input type="radio" name="m7-check-3" value="c"> C. Yes, but only if every participant has exactly four weekdays</label>
<label><input type="radio" name="m7-check-3" value="d"> D. No, because Spearman–Brown projections can never inform measurement planning</label>
<p class="scored-quiz__feedback" aria-live="polite" hidden></p>
</fieldset>

<fieldset data-answer="a" data-explanation="The pre-exclusion dataset and separate flags preserve evidence for each rule, reveal overlapping failures, support approved revisions without reconstructing unrelated steps, and explain how the analytic sample was formed.">
<legend><span>4</span> Why save a pre-exclusion dataset and separate failure flags?</legend>
<label><input type="radio" name="m7-check-4" value="a"> A. To audit each rule, quantify overlaps, support approved revisions, and explain the analytic sample</label>
<label><input type="radio" name="m7-check-4" value="b"> B. To make the final dataset larger by duplicating excluded observations</label>
<label><input type="radio" name="m7-check-4" value="c"> C. To avoid documenting the exclusion criteria in the do-file</label>
<label><input type="radio" name="m7-check-4" value="d"> D. To replace the need for source files and merge logs</label>
<p class="scored-quiz__feedback" aria-live="polite" hidden></p>
</fieldset>

<div class="scored-quiz__actions">
<button type="submit" class="button button-primary">Check answers</button>
<button type="reset" class="button button-secondary">Reset and retry</button>
</div>
<p class="scored-quiz__score" role="status" tabindex="-1" hidden></p>
</form>

<figure class="media-feature wide">
<video class="course-video" controls preload="none" playsinline poster="images/module-7-hero.webp">
<source src="media/module-7-final-dataset-in-stata.mp4" type="video/mp4" />
<track kind="captions" src="captions/module-7-final-dataset-in-stata.vtt" srclang="en" label="English" />
Your browser does not support the video tag. You can download the Module 7 video from <a href="media/module-7-final-dataset-in-stata.mp4">this link</a>.
</video>
<figcaption>Module 7 video: building the final analysis dataset in Stata.</figcaption>
</figure>
<div class="video-summary">
<p class="video-summary__label">Quick summary <span>Review aid</span></p>
<p>Build the final Stata dataset by standardizing IDs, appending batches, merging person-, day-, and night-level outputs, generating required bout and valid-day variables, evaluating reliability, and applying documented exclusions. Save a dated dataset together with the do-file, data dictionary, cleaning log, exclusion table, and README.</p>
</div>
<!-- transcript:module-7-final-dataset-in-stata -->
<details class="video-transcript" data-video="module-7-final-dataset-in-stata">
<summary>Read the full video transcript <span>Timestamped · English</span></summary>
<p class="video-transcript__tools"><a href="captions/module-7-final-dataset-in-stata.vtt" download>Download English captions (.vtt)</a></p>
<div class="video-transcript__body">
<p><span class="transcript-time">00:00</span> Welcome to Module 7, Final Dataset in Stata. In Module 6, you cleaned and standardized each GGIR output. Now you&#x27;ll bring those pieces together, appending batches, merging person-, day-, and night-level files, deriving your analysis</p>
<p><span class="transcript-time">00:14</span> variables, and applying reliability and exclusion rules to build one reproducible analysis-ready dataset. Module 8 then checks what you&#x27;ve learned. The whole module is one 11-step Stata plan. You set up by importing the Part 5</p>
<p><span class="transcript-time">00:30</span> files and cleaning IDs. You combine them, append the collection lots, then merge the day-level and quality files, and clean the sleep records. You derive MVPA and inactivity bouts, and count valid days. And you finalize. Evaluate</p>
<p><span class="transcript-time">00:46</span> reliability, collapse to participant-level, apply the three-parameter exclusions, and save a dated .dta. Keep this map in mind. Every clip that follows is one of these steps. Two operations do most of the work and it&#x27;s</p>
<p><span class="transcript-time">01:00</span> vital not to confuse them. Append stacks files on top of each other. It adds rows. Use it to combine collection lots into one file and keep a source batch marker so you never lose track of where a row came from. Merge joins files side-by-side</p>
<p><span class="transcript-time">01:15</span> by a key. It adds columns. A one-to-one merge links day rows by participant and date. A many-to-one merge attaches one participant-level record to every day. The key is what protects your unit of observation. Before we combine anything,</p>
<p><span class="transcript-time">01:32</span> let&#x27;s confirm what one row means. The person quality file has one row per ID, nine people. The day file has one row per participant-day, 31 rows, and isid proves that&#x27;s unique. We load the night file the same way. Now we append the lots. Batch A</p>
<p><span class="transcript-time">01:51</span> and Batch B stack into one day file and a source batch flag records which lot each row came from, 18 from A, 13 from B. isid confirms one unique row per participant-day. Next, a one-to-one merge, the day file to the cleaned sleep file</p>
<p><span class="transcript-time">02:10</span> on participant and date. We preserve, merge, and inspect _merge, how many matched, and which came from one side. Restore brings us back. Always check a merge. Now a many-to-one merge and the direction matters. In the person-level</p>
<p><span class="transcript-time">02:28</span> quality file, each participant appears once. In our working data, each participant has many day rows, so we merge many-to-one on participant ID. One quality record is broadcast onto every day for that person. Notice the keep</p>
<p><span class="transcript-time">02:43</span> using list. We pull in only the variables we need, the clipping score, the calibration error, the complete_24hcycle completeness score, and the individual quality flags, including qc_three_parameter_pass. After the merge, we</p>
<p><span class="transcript-time">02:57</span> always tabulate _merge. A value of three means the day matched a participant record. A one would mean a day with no quality data and a two, a participant with no days. Here, everything matches. Every day now carries</p>
<p><span class="transcript-time">03:14</span> the participant&#x27;s quality information, ready for filtering. With the files combined, we derive the numbers our rules will need, but we build them before we apply any cutoff. We count valid days per participant, flag weekdays versus</p>
<p><span class="transcript-time">03:29</span> weekends so we can require weekend coverage, and keep days with at least 10 hours of awake wear. Deriving first keeps the cutoffs transparent and easy to change. Now we build the day count variables the exclusion rules will</p>
<p><span class="transcript-time">03:43</span> depend on. First, we flag weekend days. We generate a weekend indicator, set to one when the day type is Saturday or Sunday. Then run bysort participant_id: generate byte n_days_from_daily = _N, the number of cleaned day rows each participant contributes.</p>
<p><span class="transcript-time">03:59</span> day rows each participant contributes. Because bysort groups the data by person, every row for that participant receives the same total. Next, still within participant, use egen with total() to sum the weekend indicator, giving n_weekend_from_daily,</p>
<p><span class="transcript-time">04:13</span> from daily, the count of valid weekend days per person. Finally, we tabulate both so we can see the distribution, how many valid days people have, and whether each has weekend coverage. Notice that nothing has been excluded yet. We are only</p>
<p><span class="transcript-time">04:28</span> measuring and deriving clean, transparent counts first, so that when we apply cutoffs later, every decision is visible and easy to justify. These per participant counts are exactly what the reliability estimate and the exclusion rule will</p>
<p><span class="transcript-time">04:41</span> later read. Before collapsing, verify. We derive day counts ourselves, but GGIR&#x27;s Part 5 already reports a count too. Trust, then verify. Match your derived count against Part 5, assert that every record merged, and check that the two counts</p>
<p><span class="transcript-time">04:59</span> disagree for nobody. Only reconciled data should move forward. Now we reconcile against GGIR&#x27;s own numbers. We merge the participant-level Part 5 summary one-to-one on participant_id. All five participants match, and assert _merge == 3</p>
<p><span class="transcript-time">05:16</span> turns that into a hard rule. If any record failed to match, Stata would stop. Correction: One participant&#x27;s day count disagrees; weekend counts agree for all. Stop and reconcile the day-count difference before proceeding.</p>
<p><span class="transcript-time">05:31</span> Reconciled data—and only reconciled data—move on. Now we change the grain. Collapsing takes many day-level rows per participant and produces one analysis-ready row each using means and counts. Clean day count, clean weekend count,</p>
<p><span class="transcript-time">05:51</span> and average bout measures all become participant-level variables. This is the shape your analysis will actually use. Here we inspect the clean participant-level counts. First, we format the two count variables</p>
<p><span class="transcript-time">06:06</span> so they display as whole numbers. A quick count confirms five participants and assert r(N) == 5 makes that explicit. If the number ever changed, the do-file would halt. isid participant_id confirms the data is now one row per person. We&#x27;ve truly left the day-level</p>
<p><span class="transcript-time">06:23</span> behind. Then we tabulate clean_day_count. Four participants have six cleaned days and one has seven. Every person clears the four-day minimum we&#x27;ll require. Next, tabulate clean_weekend_count. All five have at least one valid weekend day, so weekend coverage is satisfied too.</p>
<p><span class="transcript-time">06:42</span> These simple frequency tables are doing important work. They let you see, before any exclusion runs, exactly who would pass or fail each rule. Never apply a cutoff you haven&#x27;t first looked at in a table like this. It&#x27;s a small habit that saves you from silent mistakes downstream.</p>
<p><span class="transcript-time">06:59</span> Method note: No 10-hour awake-wear exclusion was applied in this demonstration. Treat clean_day_count as a cleaned-day count, not a valid-day count, until the study&#x27;s awake-wear rule is implemented and verified.</p>
<p><span class="transcript-time">07:17</span> named and documented. Then summarize across the key variables. The clean day count averages just over six with clean weekend count at two. Awake wear time, sleep duration, and the cumulative MVPA bout measures. One minute plus, five minute plus, and ten minute plus. All now carry participant</p>
<p><span class="transcript-time">07:37</span> level means. Look how compact this is compared to where we started. Raw sub-second acceleration became GGIR part files, which we appended, merged, cleaned, derived, reconciled, and collapsed into this. Every variable here traces back to a source file and a documented decision. Save it with</p>
<p><span class="transcript-time">07:55</span> today&#x27;s date in the file name, and this becomes your analysis-ready dataset. From here, the modeling is straightforward, because the careful data work is already done and fully traceable. One method deserves special mention: the Spearman–Brown prophecy formula. It estimates how</p>
<p><span class="transcript-time">08:13</span> reliability changes with the number of measurement days. A single day is noisy. More valid days better capture habitual activity, so reliability rises and then plateaus. The idea transfers to any study. Estimate reliability at your current number of days, project it for more or fewer, use that to</p>
<p><span class="transcript-time">08:31</span> justify your minimum valid-day rule and report it openly. It turns a valid-day cutoff into a measurement decision instead of an arbitrary one. For the course example, final inclusion uses three parameters. At least four valid days, at least 10 hours of awake wear per valid-day, and at least</p>
<p><span class="transcript-time">08:51</span> one weekend day. Keep an exclusion log that reports how many participants each rule removed, how many failed several, and the final analytic sample. And a critical point, define these rules from your data management and analysis plan, never after seeing the results. Save your outputs as a</p>
<p><span class="transcript-time">09:09</span> set, not a single file. A data.dta, a data dictionary, a cleaning log, an exclusion table, the Stata do-file, and a readme naming the source GGIR run and batch dates. Stamp the dataset with its date. For example, accelerometer_final_analysis_yyyymmdd.dta, so the exact version is never in doubt.</p>
<p><span class="transcript-time">09:34</span> That&#x27;s module seven. Your final dataset should be reproducible. One do-file rebuilds it from the raw GGIR outputs. Dated, every file carries its date, and explainable. Every variable traces back to a source and a documented decision. The Pelotas/UFPel do-file is a strong example,</p>
<p><span class="transcript-time">09:54</span> credited to Otavio Leão, Giulia Salaberry, and Charles Lucena. But treat it as an example, not a template. Your study sets its own rules.</p>
</div>
</details>


::: {.callout-warning}
## Corrections to the recorded demonstration

The corrected edition replaces two inaccurate passages from the original recording:

- The displayed audit finds **one participant whose day count disagrees**; weekend counts agree. Stop and reconcile that day-count difference before proceeding.
- The demonstration does **not** apply the 10-hour awake-wear exclusion. Treat `clean_day_count` as a count of cleaned days—not valid days—until the study's awake-wear rule has been implemented and verified.

The downloadable Stata example and the 13-step web checklist implement the safer logic.
:::

::: {.next-module}
<span>Next</span>
<a href="knowledge-checking.html"><strong>Knowledge Checking</strong></a>
<p>The final dataset should be reproducible, dated, and explainable. A collaborator should be able to open the do-file and understand exactly how raw GGIR outputs became analysis-ready variables.</p>
:::
:::

:::

:::
